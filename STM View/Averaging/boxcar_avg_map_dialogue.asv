function boxcar_avg_map_dialogue(data)
   prompt={'Boxcar width'};
   name='Boxcar Average';
   numlines=1;
   defaultanswer= {'1'};
   answer = inputdlg(prompt,name,numlines,defaultanswer); 
   width = str2double(answer);   
   boxcar_avg_map2(data,width);
end